# Copy this file to .env and fill in real values before running docker-compose up.
# .env is already ignored by git (see .gitignore) — never commit real credentials.

# API key for the LLM inference endpoint used by AGENT_PLANNER_MODE=llm (apps/web/lib/llm-planner.ts).
LLM_API_KEY=
